{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["OS", "RUNNER_OS"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    }
  },
  "ui": "stream"
}
